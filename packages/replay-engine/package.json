{
  "name": "@tracevault/replay-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Replay engine for constructing execution timelines from canonical events",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@tracevault/event-schema": "workspace:*",
    "@tracevault/common": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^2.0.0"
  }
}
