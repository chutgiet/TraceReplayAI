{
  "name": "@tracereplay/common",
  "version": "0.1.0",
  "private": true,
  "description": "Shared utilities, types, and constants for TraceReplay AI",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@tracereplay/event-schema": "workspace:*",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/pg": "^8.11.0",
    "typescript": "^5.4.0",
    "vitest": "^2.0.0"
  }
}
