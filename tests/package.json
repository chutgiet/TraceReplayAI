{
  "name": "@tracereplay/tests",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cross-cutting tests and fixtures for TraceReplay AI",
  "scripts": {
    "test": "vitest run",
    "build": "echo 'no build step for tests'"
  },
  "dependencies": {
    "@tracereplay/event-schema": "workspace:*",
    "@tracereplay/replay-engine": "workspace:*",
    "@tracereplay/common": "workspace:*",
    "@tracereplay/sdk-typescript": "workspace:*",
    "fastify": "^5.0.0",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "@types/pg": "^8.11.0",
    "typescript": "^5.4.0",
    "vitest": "^2.0.0"
  }
}
