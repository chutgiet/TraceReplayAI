{
  "name": "@tracereplay/evidence-service",
  "version": "0.1.0",
  "private": true,
  "description": "Evidence bundle assembly and audit service for TraceReplay AI",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@tracereplay/common": "workspace:*",
    "@tracereplay/event-schema": "workspace:*",
    "@tracereplay/graph-model": "workspace:*",
    "@tracereplay/redaction": "workspace:*",
    "@tracereplay/replay-engine": "workspace:*",
    "fastify": "^5.0.0",
    "pdfkit": "^0.18.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/pdfkit": "^0.17.5",
    "tsx": "^4.0.0",
    "typescript": "^5.4.0",
    "vitest": "^2.0.0"
  }
}
